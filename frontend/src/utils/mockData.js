// Schema reference — all data comes from backend/blockchain, never hardcoded
// These empty arrays define the expected shape for each data type

export const mockIdentities = [];
export const mockAssets = [];
export const mockDocuments = [];
export const mockVersions = [];
export const mockAuditEvents = [];
export const mockRecoveryProviders = [];

export const dashboardStats = {
  totalIdentities: 0,
  activeIdentities: 0,
  totalAssets: 0,
  totalDocuments: 0,
  verifiedDocs: 0,
  pendingProofs: 0,
  merkleRoots: 0,
  polygonAnchors: 0,
};
